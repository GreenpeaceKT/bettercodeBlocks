version = "1.1.0"

description = "Appear Syntax highlight!!!!!!"

aliucord {

    changelog.set("""
        # v0.1.0 beta
        * published
        # v1.0.0
        * released
        * diff,md,fixを言語として追加
        * いくつかの言語の色彩を変更
        * 軽微な不具合を修整
        # v1.1.0
        * 言語表示削除
        
    """.trimIndent())
    
    excludeFromUpdaterJson.set(true)
}